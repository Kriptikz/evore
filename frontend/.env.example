# Evore Frontend Environment Configuration
# Copy this to .env.local and fill in your values

# ============================================================================
# ORE Stats API (REQUIRED)
# ============================================================================
# Primary data source for all read operations (accounts, balances, metrics)
NEXT_PUBLIC_API_URL=https://your-ore-stats-domain.com

# ============================================================================
# Wallet RPC (REQUIRED)
# ============================================================================
# Used ONLY for transaction operations (signing, sending, confirming)
# Use a basic/free endpoint - no heavy reads go through this
NEXT_PUBLIC_RPC_URL=https://api.mainnet-beta.solana.com

# ============================================================================
# Deployer Settings (for AutoMiner creation)
# ============================================================================
NEXT_PUBLIC_DEPLOYER_PUBKEY=your_deployer_pubkey_here
NEXT_PUBLIC_DEPLOYER_BPS_FEE=500
NEXT_PUBLIC_DEPLOYER_FLAT_FEE=715
